server.port=8080

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/productivity_dashboard
spring.datasource.username=postgres
spring.datasource.password=saiharsha12
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# CORS configuration
# We will configure it globally in a WebMvcConfigurer bean to allow localhost:5173

# API Configurations
openai.api.key=${OPENAI_API_KEY:}
openai.model=${OPENAI_MODEL:gpt-4o-mini}
weather.api.key=${OPENWEATHER_API_KEY:}
news.api.key=${NEWS_API_KEY:}
